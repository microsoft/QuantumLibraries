namespace Microsoft.Quantum.Canon {

    function Fst<'T, 'U>(pair : ('T, 'U)) : 'T {
        let (fst, snd) = pair
        return fst
    }

    function Snd<'T, 'U>(pair : ('T, 'U)) : 'T {
        let (fst, snd) = pair
        return snd
    }

}