steps:

- powershell: ./build.ps1
  displayName: "Building"
  workingDirectory: $(Libraries.WorkingDirectory)/Build


- powershell: ./test.ps1
  displayName: "Testing"
  workingDirectory: $(Libraries.WorkingDirectory)/Build
  condition: and(succeeded(), ne(variables['Skip.Tests'], 'true'))


- powershell: ./pack.ps1
  displayName: "Pack"
  workingDirectory: $(Libraries.WorkingDirectory)/Build


- template: step-wrap-up.yml
